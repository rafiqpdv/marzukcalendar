import React from 'react'
import ReactDOM from 'react-dom/client'
import MarzukCalendar from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MarzukCalendar />
  </React.StrictMode>,
)
